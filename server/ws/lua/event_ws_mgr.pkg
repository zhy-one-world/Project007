

namespace hld
{
    struct  s_item_info;

	class event_ws_mgr
	{
     public:

		static event_ws_mgr& get_instance();
		

		void send_notice_to_all(int32 notice_id, int32 notice_create_time, guid_64 sender_guid, std::string final_string,int32 sender_template_id);	


        void send_mail_to_player_by_system(guid_64 terget_player_guid,
			int32 target_server_id,
			std::string sender_name,
			std::string title,
			std::string content_text,
			int32 money_typ1,
			int32 money_num1,
			int32 money_typ2,
			int32 money_num2,
			const std::vector<s_item_info>& item_list);	

        void send_notice_with_param(guid_64 sender_guid, const char* sender_name, const char* content_text, 
			const std::vector<int32>& param_array, int32 param_type, int32 chat_type, int32 sender_template_id = 0, int32 notice_id = 0);			
		
		s_item_info* get_free_item();
		std::vector<s_item_info>*  get_free_list();
		void  add_free_item(s_item_info* free_item); 
        std::vector<int32>*  get_free_param();	
        void  add_free_param(int32 value);		
	};
}




