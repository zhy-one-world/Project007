/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class connection_mgr:public common::singleton<connection_mgr>
	{
		public:
			void send_to_ws(const void* data_ptr, size_t data_len, int server_id = 0);
	};
}