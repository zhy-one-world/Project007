namespace hld
{

	class feather_heart_mgr
	{

	public:
        void  set_feather_heart_attr(int template_id, int level, bool is_add);
		void  set_feather_heart_attr_all(bool is_add);
		void  set_feather_heart_attr_all(citem &item, bool is_add);
		void  put_feather_heart_in_bag(int template_id, int level);

        void sync_ranking_feather_heart_data();

        void  add_buff_att(int template_id, int level,int add_level);
		void  dec_buff_att(int template_id, int level,int add_level);
	};

}