/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class player_arena_mgr
	{
	public:
		int	current_rank();
		void get_arena_rank(); 
		void req_get_match_player();  
		void req_get_first_three_player(); 
		void set_is_requiring(bool is_requiring);     
		bool is_requiring();
		bool lua_req_challenge_to_ws(int target_guid_a, int target_guid_b, int money_cost_type, int money_cost_value, int target_cur_pos, int self_cur_pos);
		bool lua_set_reward_time();
		void get_arena_rank_end(int32 arena_rank, int32 is_request);
		void do_challenge(guid_64 target_role_guid, int32 robot_id, int32 req_result,int32 cost_money_type, int32 cost_money_num,int32 target_cur_pos, int32 self_cur_pos);
		void lua_req_match_player_end(const char *data_ptr, int32 data_len);
	}
	
}
