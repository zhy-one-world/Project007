/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class recycle_mgr
	{
	public:
	    bool load_recycle_by_db_lua(const char *data_ptr, int data_len);
		bool load_recycle_task_by_db_lua(const char *data_ptr, int data_len);
		bool load_recycle_invited_by_db_lua(const char *data_ptr, int data_len);
		void on_event(e_recycle_task_type task_type, int add_num = 1, int map_id = 0);
		int get_delta_time();
		int get_time_zone();
		recycle_invited& get_recycle_invited_obj();
		recycle_task&  get_recycle_task_by_id(int recycle_id);
		recycle& get_recycle_obj();
		
		void on_process_invited_reward(string& s_guid);
	};
}