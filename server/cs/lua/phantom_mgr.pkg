/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class cphantom_mgr
	{
	public:
	    bool load_phantom_by_db_lua(const char *data_ptr, int data_len);
        void on_item_oper(citem* changed_item_ptr, int item_template_id);
		void active_skill(int skillid);
		cphantom& lua_find_object(int phantom_id);
	};
}