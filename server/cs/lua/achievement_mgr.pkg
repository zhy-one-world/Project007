/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class cachievement_mgr
	{
	public:
	    bool load_achievement_by_db_lua(const char *data_ptr, int data_len);
		void player_changed(e_achievement_type operate_type);
		void item_changed(citem* changed_item_ptr,e_item_operation operation_type);
		
		
		void save_achievement_log(int achievement_id);
		
		
		cachievement* find_achievement(int achievement_id);
		
		
		void send_one_achievement(cachievement& my_achievement);
		
		void set_can_receive_achievement_success();
	}
	
}