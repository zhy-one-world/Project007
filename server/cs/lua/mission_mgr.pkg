/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class cmission_mgr
	{
	public:
		void	target_check(e_mission_end_type mission_end_type, int target_info_0, int target_info_1);
		void    init_daily_mission();
	    void	init_against_mission();

	    cmission* find_mission_by_index(int mission_index);

	    bool    random_daily_mission(int force_mission_id = 0);
	    bool    random_against_mission(int force_mission_id = 0);
	    bool    random_marry_mission(bool is_operate, int force_mission_id = 0);
		
		void    clear_marry_mission();

	    void    reset_mission_chapter(bool is_need_send = true);

	    void    reset_mission_chapter(bool is_need_send = true);
		
		bool    load_mission_by_db_lua(const char *data_ptr, int data_len);
		
		void	refresh_missions();

	    void	refresh_side_mission(int trigger_id);
	    void	refresh_grade_mission();

	    int     get_cur_mission_chapter_id();

	    void    operate_npc_show(MissionTemplate* OldTemplatePtr, MissionTemplate* NewTemplatePtr);

	    void    operate_npc_show_by_lua(int old_mission_id, int new_mission_id);
		
		void    set_power_up_be_use(int32 power_type);
	
	}
	
}