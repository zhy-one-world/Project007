/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class ctitle_mgr
	{
	public:
       ctitle get_title_by_template_id(int template_id);
	   bool  load_title_by_db_lua(const char *data_ptr, int data_len);
	   void  ranking_first_changed(int32 ranking_type, bool is_first, bool is_notice);
	   void  set_title_no_need_see();
	};
}