/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class pawn_att
	{
	public:
	    bool  load_base_att_by_db_lua(const char *data_ptr, int data_len);
		void apply_att_change_effect(int att_level, int id, float value, float percent, int is_show, bool apply);
		void set_game_att(int att_index, float value, bool apply);
		int get_unit_base_att(int att_index);
		void send_base_att_one(int att_index);
		void set_unit_base_att(int att_index, int iValue);
	}
	
}