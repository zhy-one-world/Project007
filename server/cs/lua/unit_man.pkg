/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class unit_man
	{
	public:
		static player& get_player(const int& array_index);
		static npc& get_npc(const int& array_index);
		static unit& get_unit(const int& array_index);
		static player& get_player(const guid_64& unit_guid);
		static void set_all_player_buff(int32 buff_template_id, bool is_add);
	}
}

