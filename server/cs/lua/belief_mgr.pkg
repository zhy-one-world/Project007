/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class cbelief_mgr
	{
	public:
	    bool load_belief_by_db_lua(const char *data_ptr, int data_len);
		cbelief* getbelief(int belief_type);
		void sync_ranking_belief_data();
		int get_cur_max_rank_value(int target_star);
	}
	
}