/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class skill_manager
	{
	public:
		skill_set&	get_skill_set(const int unit_array_index);
		void	skill_change_msg(int unint_index, int skill_template_id, int pos_index);
		void							skill_spell(use_skill_param& param);
		void							skill_cast(use_skill_param& param);
		void							skill_hurt(use_skill_param& param);
		bool                            load_skill_by_db_lua(int unit_array_index, const char *data_ptr, int data_len);
		
		
	}
}