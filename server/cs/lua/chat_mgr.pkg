/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class cchat_mgr
	{
	public:
		void send_notice_new(const int&  notice_template_id, const s_chat_notice_info& notice_data);
		void send_notice(int notice_template_id, std::string show_string);
		void receive_top_tip(int string_id, int top_tip_type = e_top_tip_type_normal);
		
		bool load_chat_record_by_db_lua(const char *data_ptr, int data_len);
		
		void receive_notice(int32 notice_template_id, guid_64 sender_guid, int32 create_time);
		void send_failed_process(int32 send_result, int32 chat_type);
		
		void chat_oper(int32 pos, string record);
		
	};
}