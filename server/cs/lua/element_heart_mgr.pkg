/*
	lua 调用C++函数统一处理接口
*/
namespace hld
{
	class celement_heart_mgr
	{
	public:
		bool set_element_heart_one_att(citem& element_heart, bool is_add);
		bool set_element_heart_all_att(bool is_add);
		void put_merge_item_into_bag(int merge_id,int merge_level);
		void refresh_rune_buff();
		void sync_rune_ranking_data();
		void auto_get_equip_slot();
		int get_current_equipped_heart_num(int heart_quality_level);
	}
}

